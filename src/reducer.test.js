// import { attack } from './reducer';

// TODO mock getTurnData and test attack function
describe('attack', () => {
  test('state should be ... if player wins round', () => {
  });
  
  test('state should be ... if monster winse round', () => {
  });
})